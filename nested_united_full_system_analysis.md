# تحليل النظام الشامل وتوثيق البنية البرمجية المتكاملة لمشروع NestedUnited

هذا المستند هو مرجع شامل وتفصيلي بالكامل لبنية وتفاصيل مشروع **NestedUnited**، يدمج كافة الأجزاء الأربعة لتحليل هيكل المجلدات، والتقنيات البرمجية، والبوابة الأمنية، وتفاصيل قاعدة البيانات والجداول الـ 54، والباك إند والـ APIs، وبنية تطبيق سطح المكتب Electron، وتكاملات المزامنة مع المنصات الخارجية، ونظام الواجهات الرسومية والصلاحيات المرئية.

---

## 1. الهيكل العام والتقنيات والمجلدات

### 1.1 مقدمة عن المشروع (Project Introduction)
مشروع **NestedUnited** هو نظام متكامل لإدارة العقود، والوحدات السكنية، والحجوزات، والصيانة، بالإضافة إلى نظم فرعية لإدارة الموارد البشرية (HR)، المحاسبة والنظام المالي (Accounting)، وإدارة علاقات العملاء (CRM).
يتميز النظام بأنه تطبيق هجين يعمل كـ **Next.js Web App** وكـ **Electron Desktop Application** في نفس الوقت، مما يمكنه من التفاعل مباشرة مع المواقع الخارجية مثل Airbnb و Gathern عبر متصفحات مدمجة داخلية (WebViews/Puppeteer) لغرض مزامنة الحجوزات والرسائل لحظياً.

### 1.2 البنية التقنية (Technology Stack)
النظام مبني على مجموعة من التقنيات الحديثة والمتوافقة للعمل محلياً وسحابياً:

* **الإطار الأساسي (Core Framework):** Next.js (الإصدار 16.2.4) مع React 19.
* **البنية الأساسية لسطح المكتب (Desktop Integration):** Electron (الإصدار 41.3.0) مع `electron-builder` لبناء الحزم التنفيذية لنظام التشغيل Windows (.exe).
* **قاعدة البيانات (Database):** MySQL (تدار محلياً عبر XAMPP على المنفذ 3306) باستخدام مكتبة `mysql2` للاتصال وإدارة حمامات الاتصال (Connection Pooling).
* **نظام التحقق والهوية (Authentication):** NextAuth.js (الإصدار 4.24.14) باستخدام استراتيجية الـ JWT وتخزين الجلسات محلياً.
* **التشفير وحماية البيانات:** `bcryptjs` لتشفير كلمات المرور ومقارنتها.
* **التنسيق والتصميم (Styling):** TailwindCSS (الإصدار 4) مدمجاً مع `class-variance-authority` و `clsx` لتسهيل بناء واجهات تفاعلية وسريعة الاستجابة.
* **المزامنة وأتمتة المتصفح (Browser Automation):** Puppeteer للتحكم في المتصفحات الخارجية المستقلة، وتقنية Chrome DevTools Protocol (CDP) لاعتراض البيانات والشبكات داخل حزم Electron.
* **التواصل وتصدير البيانات:** 
  * `nodemailer` لإرسال البريد الإلكتروني الإداري.
  * `@react-pdf/renderer` لإنشاء وتوليد تقارير PDF للمرتبات والفواتير.
  * `exceljs` لتوليد ملفات Excel وتقارير الحضور والانصراف والمالية.

### 1.3 الهيكل العام للمجلدات (Root Directory Structure)
عند استعراض المجلد الرئيسي للمشروع، نجد العناصر التالية:

```
nested-united-dev/
├── .env.local                  # ملف متغيرات البيئة (قاعدة البيانات، NextAuth، إلخ)
├── .gitignore                  # الملفات المستثناة من التتبع في Git
├── README.md                   # دليل التشغيل والاستخدام للمطورين
├── package.json                # تعريف المشروع، الحزم والتبعيات، والسكربتات الأساسية
├── tsconfig.json               # إعدادات لغة TypeScript الخاصة بمشروع Next.js
├── next.config.ts              # إعدادات خادم Next.js (توليد standalone، تعطيل معالجة الصور)
├── postcss.config.mjs          # إعدادات PostCSS لمعالجة أكواد TailwindCSS
├── eslint.config.mjs           # إعدادات مدقق الأكواد وتصحيح الأخطاء الإملائية
├── jest.config.js              # إعدادات بيئة اختبارات الاستقرار
├── vitest.config.ts            # إعدادات إطار الاختبار السريع Vitest
├── vitest.setup.ts             # إعداد تهيئة بيئة الاختبار
├── proxy.ts                    # بوابات المراقبة وفلترة الطلبات والتحقق من الصلاحيات (Middleware)
├── app/                        # مجلد صفحات Next.js والمنافذ (Routes/API)
├── components/                 # مجلد المكونات المشتركة لواجهة المستخدم (UI Components)
├── lib/                        # مجلد الدوال المساعدة، اتصالات قاعدة البيانات، وإدارة الصلاحيات
├── types/                      # ملفات التعريفات الإضافية للـ TypeScript
├── electron/                   # مجلد خادم وتطبيقات تطبيق سطح المكتب Electron
├── public/                     # الملفات الثابتة والصور والشعارات والخطوط
└── tools/                      # سكربتات الصيانة، إعداد قاعدة البيانات، وإصلاحها
```

### 1.4 نظام إعادة التوجيه والحماية (Routing & Middleware)
النظام يستخدم ملف `proxy.ts` كـ Middleware داخلي لحماية جميع المسارات.

#### كيف يعمل نظام التحقق من الجلسات (Authentication Flow):
1. **التحقق الأول (NextAuth JWT):** يقرأ الـ Token المشفر من المتصفح باستخدام مفتاح التشفير `NEXTAUTH_SECRET`.
2. **التحقق الثاني (Database Sync Validation):** يتم الاستعلام عن رقم المستخدم الفرعي `token.id` داخل جدول `users` في قاعدة البيانات للتأكد من أن حسابه لم يُحذف ولم يُلغ تنشيطه (`is_active = 1`).
   * *إذا كانت الجلسة صحيحة ولكن الحساب محذوف أو معطل، تُبطل الجلسة فوراً ويُحول المستخدم لصفحة الدخول لمنع حلقات التوجيه اللانهائية (307 Redirect Loop).*
3. **توزيع الصلاحيات (RBAC - Role Based Access Control):**
   * **المسارات العامة:** `/login` متاحة للجميع (وإذا كان المستخدم مسجلاً بالفعل، يتم نقله للـ Portal).
   * **المسار الرئيسي (Portal):** المسار `/portal` متاح لكل من يملك حساباً نشطاً.
   * **المسارات المحمية:** الأقسام مثل `/dashboard` و `/hr` و `/crm` و `/accounting` و `/settings` يتم فحص صلاحية الوصول إليها ديناميكياً عبر استعلام قاعدة البيانات للتأكد من امتلاك المستخدم صلاحية العرض لهذا المسار المعين.

### 1.5 هيكل مجلد الصفحات الرئيسي (The `app` Folder Structure)
يحتوي المجلد `app/` على تقسيمات فرعية لكل تطبيق:

* **`(auth)/login`**: يحتوي على صفحة تسجيل الدخول وتصميمها.
* **`portal`**: صفحة المدخل الرئيسي للنظام لاختيار التطبيق الفرعي.
* **`dashboard`**: نظام إدارة التأجير والحجوزات والصيانة وجاهزية الوحدات.
* **`hr`**: نظام إدارة شؤون الموظفين، الرواتب، الحضور والانصراف، والتقييمات.
* **`accounting`**: النظام المحاسبي، الفواتير، الحسابات، دفاتر اليومية والتقارير المالية.
* **`crm`**: نظام إدارة علاقات العملاء، العملاء المستهدفين، ومراحل الصفقات.
* **`employee`**: بوابة الخدمة الذاتية للموظف لعرض بياناته وحضوره وطلب إجازة.
* **`settings`**: إدارة إعدادات الشركة، الصلاحيات، وإدارة المستخدمين.
* **`api`**: منافذ الاتصال الخلفية (REST API endpoints) التي تخدم جميع التطبيقات الفرعية.

---

## 2. هيكل قاعدة البيانات بالتفصيل الممل

### 2.1 نظرة عامة على قاعدة البيانات (Database Architecture)
تعتمد قاعدة البيانات على محرك **InnoDB** لدعم العلاقات القيادية والـ Foreign Keys ومبدأ العمليات المترابطة (Transactions).
تم تنظيم الجداول منطقياً لتغطية ستة نطاقات رئيسية:
1. **النظام الأساسي والصلاحيات (Core & Security)**
2. **إدارة العقارات والتأجير (Rentals & PMS)**
3. **مزامنة المنصات وأتمتة المتصفح (Browser Sync & Scraping)**
4. **شؤون الموظفين والموارد البشرية (HR & Payroll)**
5. **إدارة علاقات العملاء (CRM)**
6. **المحاسبة والمالية (Accounting & General Ledger)**

### 2.2 النطاق الأول: النظام الأساسي والصلاحيات (Core & Security)

#### أ. جدول المستخدمين `users`
يمثل الحسابات التي يمكنها تسجيل الدخول إلى النظام (سواء إداريين، محاسبين، عمال صيانة، إلخ):
* `id` (char(36), PRI, Default: uuid()): المعرف الفريد الفرعي للمستخدم.
* `email` (varchar(255), UNI): البريد الإلكتروني الفريد للمستخدم وهو اسم الدخول.
* `password_hash` (varchar(255)): كلمة المرور المشفرة بخوارزمية bcrypt.
* `role` (enum('super_admin','admin','accountant','hr_manager','maintenance_worker','employee')): الدور الوظيفي الأساسي للمستخدم.
* `name` (varchar(255)): اسم المستخدم الكامل.
* `is_active` (tinyint(1), Default: 1): مؤشر حالة الحساب (نشط/معطل).
* `created_at` / `updated_at`: طوابع زمنية للإنشاء والتعديل.
* `deleted_at` (datetime): لدعم الحذف الناعم (Soft Delete) بحيث لا تُحذف البيانات التاريخية بل يتم إخفاء الحساب فقط.

#### ب. جدول صلاحيات المستخدمين الصفحية `user_permissions`
يدير صلاحيات الوصول التفصيلية لصفحات النظام لكل مستخدم على حدة:
* `id` (char(36), PRI, Default: uuid())
* `user_id` (char(36), FK -> users.id): المستخدم صاحب الصلاحية.
* `page_path` (varchar(255)): مسار الصفحة المحدد (مثال: `/hr/employees`).
* `can_view` (tinyint(1), Default: 0): صلاحية العرض.
* `can_edit` (tinyint(1), Default: 0): صلاحية التعديل والإضافة والحذف.

#### ج. جدول صلاحيات الأدوار للنظام `role_system_permissions`
يتحكم في إمكانية وصول دور وظيفي كامل (مثل hr_manager) إلى نظام فرعي كامل (مثل الموارد البشرية):
* `id` (char(36), PRI)
* `role` (varchar(50)): اسم الدور.
* `system_id` (varchar(50)): اسم النظام الفرعي (مثال: `hr` أو `accounting`).
* `can_access` (tinyint(1)): حالة إمكانية الدخول (1/0).

### 2.3 النطاق الثاني: إدارة العقارات والتأجير (Rentals & PMS)

#### أ. جدول الوحدات `units`
يمثل الشقق أو الفيلات المتاحة للتأجير:
* `id` (char(36), PRI, Default: uuid())
* `unit_name` (varchar(255)): اسم الوحدة.
* `unit_code` (varchar(100)): كود الوحدة الداخلي.
* `city` (varchar(255)): المدينة.
* `status` (enum('active','inactive'), Default: 'active'): حالة الوحدة في النظام.
* `readiness_status` (varchar(50), Default: 'ready'): حالة جاهزية الوحدة للتسكين (مثل: جاهزة، قيد التنظيف، خروج اليوم، إلخ).
* حقول الجاهزية التاريخية: `readiness_checkout_date` ، `readiness_guest_name` ، `readiness_notes` ، `readiness_updated_by` (FK -> users.id).

#### ب. جدول منصات الوحدات `unit_platforms`
يربط الوحدة بروابطها ورموزها التعريفية على المنصات الخارجية:
* `id` (char(36), PRI)
* `unit_id` (char(36), FK -> units.id)
* `platform` (enum('airbnb','gathern'))
* `listing_code` (varchar(255)): كود إدراج الوحدة على المنصة الخارجية.

#### ج. جدول الحجوزات الرسمية `bookings`
يسجل الحجوزات المدخلة يدوياً أو المحولة من نظام الصفقات:
* `id` (char(36), PRI, Default: uuid())
* `unit_id` (char(36), FK -> units.id): الوحدة المحجوزة.
* `guest_name` (varchar(255)): اسم الضيف.
* `phone` (varchar(50)): رقم هاتف الضيف.
* `checkin_date` (date) / `checkout_date` (date): تاريخ الدخول والخروج.
* `amount` (decimal): قيمة الحجز الإجمالية.
* `currency` (varchar(10), Default: 'SAR')

#### د. جدول تذاكر الصيانة `maintenance_tickets`
طلبات الصيانة المفتوحة للوحدات:
* `id` (char(36), PRI, Default: uuid())
* `unit_id` (char(36), FK -> units.id): الوحدة التي تحتاج صيانة.
* `title` (varchar(255)): عنوان الطلب.
* `description` (text): تفاصيل العطل.
* `status` (enum('open','in_progress','resolved')): حالة التذكرة.
* `priority` (enum('low','medium','high','urgent')): الأهمية.
* `created_by` (char(36), FK -> users.id): منشئ التذكرة.
* `assigned_to` (char(36), FK -> users.id): فني الصيانة المكلف.
* `accepted_at` (datetime): وقت قبول الفني للمهمة.
* `worker_notes` (text): ملاحظات الفني بعد المعاينة أو الإصلاح.

### 2.4 النطاق الثالث: مزامنة المنصات وأتمتة المتصفح (Browser Sync & Scraping)

#### أ. جدول حسابات المتصفح `browser_accounts`
يخزن تفاصيل الحسابات النشطة على المنصات الخارجية التي تدار عبر المتصفح المدمج في Electron:
* `id` (varchar(255), PRI): معرف فريد (غالباً يكون البريد الإلكتروني للحساب الخارجي).
* `platform` (enum('airbnb','gathern','whatsapp','general')): المنصة التابع لها.
* `account_name` (varchar(255)): اسم الحساب للعرض.
* `session_partition` (varchar(255)): اسم بارتشن التخزين الخاص بالـ Cookie في Electron (مثال: `airbnb_user1`).
* `ws_endpoint` (text): عنوان websocket للاتصال بمتصفح Puppeteer النشط.
* `auth_token` / `chat_auth_token` (text): التوكنز الملتقطة ديناميكياً لإجراء طلبات الـ API المباشرة.
* `is_active` (tinyint(1)): هل الحساب قيد التشغيل حالياً.

#### ب. جدول تقويمات الوحدات `unit_calendars`
يخزن روابط iCal لمزامنة حجوزات المنصات الخارجية تلقائياً:
* `id` (char(36), PRI, Default: uuid())
* `unit_id` (char(36), FK -> units.id)
* `platform` (enum('airbnb','gathern','whatsapp'))
* `ical_url` (text): رابط الـ iCal المستخرج من المنصة.
* `is_primary` (tinyint(1))

#### ج. جدول الحجوزات الخارجية `reservations`
الحجوزات المستوردة تلقائياً عبر ملفات iCal أو اعتراض طلبات الشبكة:
* `id` (char(36), PRI)
* `unit_id` (char(36), FK -> units.id)
* `platform` (enum('airbnb','gathern'))
* `start_date` (date) / `end_date` (date)
* `summary` (text): ملخص الحجز.
* `ical_uid` (varchar(255)): المعرف الفريد القادم من ملف iCal لمنع التكرار.

#### د. جدول رسائل المنصات `platform_messages`
الرسائل المستوردة من شات Airbnb و Gathern لعرضها في صندوق الوارد الموحد:
* `id` (varchar(255), PRI): كود الرسالة الفريد من المنصة.
* `platform_account_id` (char(36), FK -> browser_accounts.id)
* `thread_id` (varchar(255)): كود المحادثة.
* `guest_name` (varchar(255)): اسم العميل التابع للمحادثة.
* `message_text` (text): نص الرسالة.
* `is_from_me` (tinyint(1)): هل الرسالة مرسلة من موظفنا (1) أم من الضيف (0).
* `sent_at` (datetime): وقت إرسال الرسالة الأصلي.

### 2.5 النطاق الرابع: شؤون الموظفين والموارد البشرية (HR & Payroll)

#### أ. جدول الموظفين `hr_employees`
يحتوي على البيانات التفصيلية للموظفين (والتي ترتبط بحساب المستخدم الأساسي `users.id`):
* `id` (varchar(36), PRI): المعرف المربوط بـ `users.id`.
* `employee_code` (varchar(50), UNI): الرقم الوظيفي.
* `join_date` (date) / `contract_end_date` (date): تواريخ التعاقد.
* `basic_salary` (decimal(10,2)): الراتب الأساسي الشهري.
* `housing_allowance` / `transportation_allowance` / `other_allowances` (decimal): البدلات المختلفة.
* `gosi_registered` (tinyint(1)): هل الموظف مسجل في التأمينات الاجتماعية.
* `bank_name` / `iban` (varchar): بيانات تحويل الراتب البنكية.

#### ب. جدول الحضور والانصراف `hr_attendance`
يسجل عمليات البصمة اليومية للموظفين:
* `id` (varchar(36), PRI)
* `employee_id` (varchar(36), FK -> hr_employees.id)
* `date` (date): تاريخ اليوم المعني.
* `clock_in` (time) / `clock_out` (time): أوقات البصمة الفعلية.
* `status` (enum('present','absent','late','sick_leave','annual_leave','unpaid_leave','weekend','holiday')): حالة الموظف في اليوم.
* `is_manual` (tinyint(1)): هل تم تعديل البصمة يدوياً من قبل الإدارة.

#### ج. جدول مسيرات الرواتب `hr_payroll_runs`
يسجل دورات تصفية الرواتب الشهرية:
* `id` (varchar(36), PRI)
* `month` (int), `year` (int)
* `status` (enum('draft','approved','paid')): حالة الدورة المحاسبية للرواتب.
* `total_net_salary` (decimal(12,2)): إجمالي المبالغ المصروفة لكافة الموظفين في هذه الدورة.

### 2.6 النطاق الخامس: إدارة علاقات العملاء (CRM)

#### أ. جدول العملاء `customers`
بيانات العملاء المهتمين بحجز وحدات أو خدمات:
* `id` (char(36), PRI)
* `name` (varchar(255))
* `phone` (varchar(50)): رقم الهاتف للتواصل.
* `email` (varchar(255))

#### ب. جدول صفقات المبيعات `crm_deals`
أداة تتبع مراحل المبيعات والفرص الاستثمارية (Pipeline):
* `id` (char(36), PRI, Default: uuid())
* `customer_id` (char(36), FK -> customers.id)
* `unit_id` (char(36), FK -> units.id, Nullable): الوحدة المهتم بها العميل.
* `title` (varchar(255)): اسم الصفقة.
* `amount` (decimal(10,2)): القيمة المتوقعة للصفقة.
* `stage` (enum('new','contacting','qualified','proposal','negotiation','won','paid','completed','lost','partial_payment','management')): المرحلة الحالية في الـ Pipeline.
* `priority` (varchar(20), Default: 'medium'): الأولوية (أهمية العميل).

### 2.7 النطاق السادس: المحاسبة والمالية (Accounting & General Ledger)

#### أ. دليل الحسابات `accounting_accounts`
شجرة الحسابات المالية الكاملة (Chart of Accounts):
* `id` (varchar(36), PRI)
* `code` (varchar(50), UNI): كود الحساب (مثال: `110100` للنقدية).
* `name` (varchar(200)): اسم الحساب (بالعربية).
* `type` (enum('asset','liability','equity','income','expense')): نوع الحساب الأصلي بالميزانية أو قائمة الدخل.

#### ب. فواتير المبيعات والمشتريات `accounting_invoices`
* `id` (varchar(36), PRI)
* `invoice_number` (varchar(100), UNI): رقم الفاتورة التسلسلي.
* `partner_id` (varchar(36), FK -> accounting_partners.id): العميل أو المورد صاحب الفاتورة.
* `type` (enum('out_invoice','in_invoice','out_refund','in_refund')): اتجاه الفاتورة (مبيعات/مشتريات/مرتجع).
* `amount_untaxed` (decimal), `amount_tax` (decimal), `amount_total` (decimal): قيم الفاتورة والضرائب.
* `state` (enum('draft','posted','cancel')): حالة الفاتورة الدفترية.

#### ج. القيود المحاسبية ورؤوس الحركات `accounting_moves` & `accounting_move_lines`
تسجل الأثر المزدوج للمعاملات المالية في دفاتر اليومية العامة:
* `accounting_moves`: يمثل ترويسة القيد (رقم القيد، التاريخ، الوصف، دفتر اليومية).
* `accounting_move_lines` (حركات القيد): الحساب المدين والدائن في كل حركة:
  * `account_id` (varchar(36), FK -> accounting_accounts.id)
  * `debit` (decimal(12,2)): القيمة المدينة.
  * `credit` (decimal(12,2)): القيمة الدائنة.

---

## 3. خادم الباك إند، مسارات الـ API، وهندسة Electron

### 3.1 مسارات خادم الـ API (REST API Endpoints)
تخدم واجهات برمجة التطبيقات (APIs) جميع طلبات الويب وتتحكم في المعاملات مع قاعدة البيانات. توجد جميعها داخل مجلد `app/api/` وتعتمد على `NextResponse` لإرجاع البيانات بصيغة JSON.

#### أ. إدارة الهوية والوصول (Auth & Users)
* **`api/auth/[...nextauth]` (`route.ts`):** 
  يقوم بتهيئة NextAuth.js للتعامل مع الدخول بالبريد الإلكتروني والرقم السري. يستعلم من قاعدة البيانات ويستخدم `bcrypt.compare` للمطابقة.
* **`api/users` (`route.ts`):** 
  عرض وتعديل بيانات المستخدمين.
* **`api/users/create` (`route.ts`):** 
  إنشاء مستخدم جديد وتشفير الرقم السري الخاص به قبل الإدخال.
* **`api/users/[id]/change-password` (`route.ts`):** 
  تغيير كلمة المرور الخاصة بمستخدم محدد بعد التحقق من الصلاحيات.
* **`api/users/[id]/permissions` (`route.ts`):** 
  عرض وتعديل وتحديث حقول جدول `user_permissions` للمستخدم (صلاحيات الصفحات).

#### ب. إدارة العقارات والوحدات والحجوزات (Rentals, Bookings & iCal)
* **`api/units` (`route.ts`):** 
  * `GET`: جلب قائمة الوحدات مع تصفية حسب المدينة أو حالة الوحدة أو حساب المنصة.
  * `POST`: إنشاء وحدة جديدة وإسنادها لحساب منصة محدد.
* **`api/units/[id]` (`route.ts`):** 
  * `GET`: جلب تفاصيل وحدة معينة.
  * `PUT`: تحديث بيانات الوحدة (الاسم، الكود، المدينة، السعة، إلخ).
  * `DELETE`: حذف وحدة برمجياً من النظام.
* **`api/units/[id]/readiness` (`route.ts`):** 
  تعديل حالة جاهزية الوحدة يدوياً وتوثيق هوية الموظف الذي قام بالتحديث (`readiness_updated_by`).
* **`api/bookings` (`route.ts`):** 
  * `GET`: عرض الحجوزات المدخلة يدوياً.
  * `POST`: إنشاء حجز يدوي جديد وتثبيته في قاعدة البيانات.
* **`api/bookings/[id]` (`route.ts`):** 
  * `PUT`/`DELETE`: تعديل أو حذف الحجز اليدوي.
* **`api/sync` (`route.ts`):** 
  تفعيل محرك المزامنة يدوياً لقراءة ملفات iCal من المنصات الخارجية وتحديث جدول `reservations` وجدول `sync_logs`.

#### ج. محرك الموارد البشرية والرواتب (HR & Payroll Endpoints)
* **`api/hr/employees` (`route.ts`):** 
  إضافة وعرض الموظفين وبياناتهم المالية (المرتب والبدلات).
* **`api/hr/attendance/punch` (`route.ts`):** 
  تسجيل بصمة حضور أو انصراف لحظية للموظف الحالي وتخزينها في `hr_attendance`.
* **`api/hr/attendance/manual` (`route.ts`):** 
  تعديل بصمات الحضور للموظفين يدوياً بواسطة مديري الموارد البشرية.
* **`api/hr/payroll` (`route.ts`):** 
  * `POST`: تشغيل محرك حساب الرواتب لشهر معين (Payroll Engine)؛ يقوم باحتساب أيام الغياب والتأخر ديناميكياً من جدول البصمة وخصم نسب التأمينات والخصومات واحتساب صافي الراتب، ثم إنشاء مسير الرواتب وحفظه في جدول `hr_payroll_details`.

#### د. النظام المالي والمحاسبي (Accounting Endpoints)
* **`api/accounting/invoices` (`route.ts`):** 
  * `GET`: جلب الفواتير الصادرة والواردة.
  * `POST`: إنشاء فاتورة جديدة وحساب القيمة غير الخاضعة للضريبة والضريبة والقيمة الإجمالية تلقائياً.
* **`api/accounting/payments` (`route.ts`):** 
  تسجيل سندات القبض والصرف وتوزيع المبالغ على الفواتير المفتوحة وتوليد القيود المزدوجة المقابلة ديناميكياً.

### 3.2 بنية Electron وهندسة تشغيل البرنامج (Electron Architecture)
عند تشغيل النظام كتطبيق مكتب، يتدخل محرك Electron لإتاحة ميزات هجينة فريدة.

#### أ. ملف التشغيل الرئيسي `electron/main.ts`
يدير دورة حياة تطبيق سطح المكتب بالكامل ويحتوي على الأقسام الحيوية التالية:
1. **تشغيل خادم Next.js محلياً (Standalone Server Start):** 
   في بيئة الإنتاج (عند تعليب التطبيق)، يقوم التطبيق باستدعاء `child_process.spawn("node", [serverPath])` لتشغيل خادم Next.js المبني محلياً على المنفذ `3456` ليعمل التطبيق بالكامل محلياً بدون إنترنت خارجي.
2. **عزل الجلسات (Session Isolation):**
   * ينشئ التطبيق جلسة تخزين مستقلة للداشبورد الرئيسي (`dashboardSession = session.fromPartition("persist:dashboard-main")`).
   * ينشئ جلسات مستقلة معزولة لكل حساب Airbnb أو Gathern يتم فتحه، لمنع تداخل ملفات تعريف الارتباط (Cookies).
3. **مستشعر الرموز والتوكنز (Token Sniffer):**
   يقوم باعتراض طلبات الشبكة الصادرة من الـ WebViews المدمجة للتعرف على كود API Key و Bearer Token الخاص بـ Gathern و Airbnb، ويقوم بحفظها فوراً في قاعدة البيانات المحلية لاستخدامها لاحقاً في طلبات الباك إند الخلفية الصامتة.
4. **أيقونة النظام المصغرة (System Tray):**
   ينشئ أيقونة للنظام بجانب الساعة على نظام Windows للتحكم في فتح وإغلاق وتنشيط التطبيق وحمايته من التوقف غير المتوقع في الخلفية.

#### ب. التقاط الشبكة المتقدم `electron/cdp-interceptor.ts`
يستخدم بروتوكول **Chrome DevTools Protocol (CDP)** للارتباط المباشر بالـ WebViews على مستوى محرك Chromium الأساسي.
* **الوظيفة:** يقوم باعتراض كل البيانات الواردة (Response Payloads) من روابط API الخاصة بـ Airbnb و Gathern. بمجرد كشف بيانات الـ Inbox أو الحجوزات الواردة، يقوم بتحويل النص بصيغة JSON مباشرة لخادم التطبيق لقراءته وتخزينه، متجاوزاً بذلك تقنيات كشف الروبوتات والحماية المعقدة للمنصات.
* **الحل الذكي للتعارض:** بما أن CDP لا يسمح باتصالين متزامنين، يقوم الكود بفصل نفسه تلقائياً بمجرد قيام المطور بفتح أدوات المطور (F12/DevTools)، ويعيد الاتصال فور إغلاقها.

#### ج. خدمات البولينج والمزامنة `electron/polling-service.ts`
خدمة دورية تعمل في الخلفية لضمان تدفق البيانات لحظياً:
* تقوم بالمرور على حسابات المتصفح النشطة وإرسال نبضات دورية (Keep-Alive) لمنع انتهاء الجلسات.
* تقوم بالاستعلام الدوري عن الرسائل الجديدة والحجوزات المعلقة وتحديث الإشعارات داخل شريط المهام وتطبيق الويب.

### 3.3 محاكي ومحرك متصفح Puppeteer (`lib/browser-manager.ts`)
عندما يحتاج النظام لإجراء عمليات أوتوماتيكية متطورة خلف الكواليس (مثل الرد الآلي على الرسائل أو سحب البيانات الإحصائية التفصيلية):
* يقوم `BrowserManager.launchBrowser(accountId)` بفتح متصفح Chromium حقيقي مستقل مع تمرير مسار ملف تعريف مستخدم فريد (`userDataDir`) لكل حساب لحفظ الجلسة.
* **التخفي من كشف البوتات (Anti-Fingerprinting / Stealth):**
  * يتم تشغيل المتصفح بـ arguments مخصصة مثل `--disable-blink-features=AutomationControlled` لحذف مؤشرات التحكّم التلقائي.
  * يتم حظر تمرير `--enable-automation` in Chromium.
  * يتم تمرير User Agent حقيقي مطابق لمتصفح Chrome العادي على أنظمة Windows.

---

## 4. الواجهات الرسومية، المكونات، ونظام الصلاحيات المرئي

### 4.1 الهيكل العام للواجهة الرسومية (Layout & Navigation Shell)
يعتمد النظام على هيكل تصميم موحد وسريع الاستجابة يعزز سهولة التنقل بين الأنظمة الفرعية المختلفة.

#### أ. قشرة التطبيق الأساسية `components/layout/AppShell.tsx`
هي الحاوية الرئيسية لكافة واجهات الإدارة:
* تقوم باحتواء الـ **Sidebar** والـ **Header** والـ **Content Area**.
* تضمن تناسق الهوامش والخطوط وعرض التنبيهات المنبثقة (Toasts) والإشعارات اللحظية.

#### ب. شريط القوائم الموحد `components/layout/UnifiedSidebar.tsx`
شريط التنقل الجانبي الذكي الذي يتأقلم وفقاً للقسم الذي يتواجد فيه المستخدم:
* **التأقلم الديناميكي:** يقرأ المسار الحالي (`pathname`) ويقوم بتحميل مصفوفة التنقل المخصصة للقسم المعني تلقائياً (مثال: إذا كان العميل يتصفح المحاسبة، يعرض شريط المالي المتمثل في `ACCOUNTING_NAV`؛ وإذا كان في الموارد البشرية، يعرض قائمة الموظفين والرواتب `HR_NAV`).
* **إخفاء الحقول غير المصرح بها:** يستند إلى صلاحيات دور المستخدم وجدول الصلاحيات الفردية لإخفاء أو تعطيل أزرار المسارات التي لا يملك الموظف حق الوصول إليها.

#### ج. محول الأنظمة الفرعية `components/layout/AppSwitcher.tsx`
مكون مرئي يظهر في الترويسة أو القائمة الجانبية:
* يتيح للمستخدمين ذوي الصلاحيات المتعددة الانتقال فوراً وبنقرة واحدة بين نظام إدارة التأجير، الموارد البشرية، المحاسبة، والـ CRM دون الحاجة لتسجيل الخروج والعودة للـ Portal الرئيسي.

#### د. شريط التبويبات المتعددة `components/layout/TabBar.tsx`
شريط يقع أعلى مساحة العمل لمحاكاة تجربة أنظمة التشغيل المكتبية:
* يتيح فتح عدة نوافذ وتصفح شاشات مختلفة في نفس الوقت، مع الحفاظ على حالة المدخلات داخل التبويبات المفتوحة في الخلفية لسرعة العمل.

### 4.2 المكونات التفاعلية الأساسية (Interactive Components)

#### أ. محرك البحث العام الموحد `components/layout/GlobalSearch.tsx`
مربع بحث ذكي وسريع متواجد في الترويسة:
* يقوم بالاستعلام المتزامن في عدة جداول دفعة واحدة (الوحدات، الحجوزات، الموظفين، العملاء، والفواتير المالية) ليعرض للمستخدم نتائج فورية مقسمة حسب النوع مع روابط سريعة للوصول للهدف.

#### ب. شريط حالة المزامنة `components/SyncStatus.tsx`
مكون حيوي يظهر في لوحة التحكم الرئيسية للتأجير:
* يعرض توقيت آخر عملية مزامنة ناجحة للحجوزات.
* يعرض مؤشراً مرئياً لحالة الاتصال بالمنصات.
* يحتوي على زر يدوي لتفعيل المزامنة المباشرة لقراءة التقويمات (`SyncButton.tsx`).

#### ج. مكون التحكم بجاهزية الوحدات `app/dashboard/unit-readiness/`
واجهة تفاعلية تمكن المشرفين من متابعة وتحديث جاهزية الشقق بعد خروج النزلاء:
* يعرض الشقق كبطاقات ملونة ترمز لحالة النظافة والصيانة (أحمر: قيد التنظيف، أخبر: جاهزة، أصفر: خروج اليوم).
* يتيح للمشرف كتابة ملاحظات وتكليف عامل صيانة مباشرة بمهمة نظافة أو إصلاح معينة.

#### د. نموذج الفواتير المعقد `components/accounting/InvoiceForm.tsx`
من أهم المكونات البرمجية بالسيستم:
* يدير إدخال الفواتير المحاسبية بدقة تامة.
* يقوم بحساب السطور والخصومات ونسب الضريبة (VAT 15%) وتحديث المجاميع ديناميكياً في الواجهة أثناء الكتابة.
* يدير البحث المباشر عن العملاء والموردين وربط السطور بدليل الحسابات ومراكز التكلفة المحددة.

### 4.3 حماية الواجهات ونظام الصلاحيات المرئي (Permission Guarding)

#### أ. حارس الصلاحيات المكوناتي `components/PermissionGuard.tsx`
مكوّن تغليف (Wrapper Component) يُستخدم لحماية عناصر مرئية محددة داخل الصفحات:
* **طريقة العمل:** يتم تمرير مسار الصفحة ونوع الصلاحية المطلوبة (مثل: عرض `view` أو تعديل `edit`) كخصائص للمكون:
  ```tsx
  <PermissionGuard pagePath="/accounting/invoices" permission="edit">
      <button>إنشاء فاتورة جديدة</button>
  </PermissionGuard>
  ```
* إذا كان المستخدم الحالي لا يملك صلاحية التعديل للمسار المذكور، يقوم المكون بإخفاء الزر تماماً من الواجهة الرسومية لمنع محاولات التعديل غير المصرح بها.

#### ب. الخطاف البرمجي للصلاحيات `lib/hooks/usePermission.ts`
دالة برمجية مخصصة (Custom Hook) تُستخدم داخل مكونات React للتحقق البرمجي السريع:
* تعطي المطور إمكانية التحقق من الصلاحيات برمجياً داخل منطق الدوال البرمجية (مثال: فحص الصلاحية قبل تنفيذ أمر الحذف أو التعديل وإظهار رسالة خطأ للنزيل في حال عدم امتلاك الصلاحية).

---
**تم دمج وتوثيق كافة العناصر والأنظمة البرمجية بنجاح.**
