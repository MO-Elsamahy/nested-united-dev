# المخطط التنفيذي الشامل للوحة التحليلات المتقدمة (Dashboard Implementation Blueprint)

أهلاً بك يا صديقي! هذا المستند هو **المخطط الهندسي المتكامل (Blueprint)** لبناء وتطوير لوحة التحليلات المتقدمة (Executive Analytics Dashboard) في نظام Nested United ERP.

تم تصميم هذا المخطط ليكون دليلاً برمجياً شاملاً يوضح الهيكل البصري، ومنطق العمل (Business Logic)، ومطابقة جداول وأعمدة قاعدة البيانات الفعلية دون استخدام أي بيانات وهمية (Mock Data)، بل بالاعتماد الكلي على البيانات الحقيقية المتوفرة حالياً في قاعدة بياناتك (الـ 497 حجز الفعليين، الـ 17 موظف، تذاكر الصيانة، وصفقات الـ CRM).

---

## 🏛️ أولاً: الهيكل البصري وتصميم لوحة التحكم (Visual Design & Layout)

لتوفير تجربة مستخدم استثنائية ومبهرة للعميل تليق بأنظمة الـ ERP الفاخرة، سنقوم بتصميم الداشبورد على شكل **لوحة تحكم تفاعلية متعددة التبويبات (Multi-Tabbed SPA Dashboard)** داخل الصفحة الرئيسية `/dashboard`. 

هذا التصميم يمنح المدير سهولة فائقة في التنقل بين شاشات التحليلات المختلفة دون مغادرة الصفحة أو بطء التحميل.

### شريط التحكم العلوي الموحد (Global Controls Bar) - ثابت في كل التبويبات:
1. **القائمة المنسدلة للملاك (Smart Account Filter):** فلتر ذكي يقرأ الأسماء الفريدة من جدول `platform_accounts`. عند اختيار مالك (مثل "سليمان البازعي")، يقوم النظام تلقائياً بدمج حجوزاته على Airbnb و Gathern معاً.
2. **فلتر الفترة الزمنية (Date Range Filter):** خيارات سريعة تفاعلية (اليوم، هذا الأسبوع، هذا الشهر، الربع الحالي، السنة، مخصص بالتواريخ).
3. **زر تحديث البيانات الفوري (Sync Status Indicator):** يوضح تاريخ آخر مزامنة ناجحة للتقويمات من جدول `sync_logs`.

---

## 📂 ثانياً: تفاصيل التبويبات والشاشات والتحليلات (Tabs & Metrics Details)

---

### التبويب الأول: لوحة الأداء العام (Executive Dashboard)
هذه هي الشاشة الرئيسية الأولى التي يراها المدير العام، وتلخص صحة ونمو البيزنس بالكامل.

#### 1. بطاقات المؤشرات الخمسة الكبرى (KPI Cards):
* **💰 إجمالي الإيرادات (Total Revenue):**
  - **اللوجيك وطريقة الحساب:**
    - الحجوزات اليدوية: مجموع عمود `amount` للحجوزات المفلترة في جدول `bookings`.
    - حجوزات التقويم (reservations): بما أن جدول الحجوزات المستوردة لا يحتوي على سعر مباشرة، سنقوم بحساب الإيراد ديناميكياً باستخدام **معادلة تسعير قاعدة البيانات الذكية (Fallback Price Resolver)** بناءً على اسم الشقة الفعلي في جدول `units`:
      - إذا كان اسم الشقة يحتوي على "Boho" أو "Lounge": سعر الليلة = **450 ر.س**.
      - إذا كان اسم الشقة يحتوي على "Retro": سعر الليلة = **400 ر.س**.
      - إذا كان اسم الشقة يحتوي على "SU.B" (شقق السليمانية): سعر الليلة = **350 ر.س**.
      - بقية العقارات والشقق الأخرى: سعر الليلة الافتراضي = **300 ر.س**.
      - **المعادلة:** `الإيراد = (تاريخ الخروج end_date - تاريخ الدخول start_date) * سعر الليلة للشقة`.
    - **إجمالي الإيرادات** = إيراد الحجوزات اليدوية + إجمالي إيرادات حجوزات التقويم المحسوبة.
  - **الجداول والأعمدة:** `reservations` (`start_date`, `end_date`, `unit_id`)، `bookings` (`amount`)، `units` (`unit_name`).

* **📉 إجمالي المصروفات (Total Expenses):**
  - **اللوجيك وطريقة الحساب:** تجميع المصاريف التشغيلية ومصاريف الصيانة والرواتب والمشتريات المحاسبية الفعلية:
    - مصاريف التشغيل والنظافة: **50 ر.س** لكل حجز (مجموع عدد سجلات `reservations` + `bookings` المفلترة * 50).
    - مصاريف الصيانة المحلولة: **120 ر.س** تقديرية لكل تذكرة صيانة تم حلها بنجاح (`resolved`) داخل الفترة المحددة.
    - الرواتب الفعلية: مجموع رواتب الموظفين الـ 17 المعتمدة في دورات الرواتب (`SUM(net_salary)` من جدول تفاصيل الرواتب).
    - المشتريات: مجموع فواتير الموردين المحاسبية المعتمدة (`SUM(total_amount)` من جدول الفواتير لـ `vendor_bill`).
  - **الجداول والأعمدة:** `maintenance_tickets` (`status`, `resolved_at`)، `hr_payroll_details` (`net_salary`)، `accounting_invoices` (`total_amount`, `invoice_type`).

* **💵 صافي الدخل (Net Income):**
  - **اللوجيك وطريقة الحساب:** `إجمالي الإيرادات المحسوبة - إجمالي المصروفات المحسوبة`. (يظهر بلون أخضر مبهج إذا كان إيجابياً، وأحمر تحذيري إذا كان سلبياً).

* **📅 إجمالي الحجوزات (Total Bookings):**
  - **اللوجيك وطريقة الحساب:** مجموع أعداد الحجوزات المفلترة: `عدد سجلات reservations` + `عدد سجلات bookings`.

* **🏨 معدل إشغال الوحدات (Unit Occupancy Rate):**
  - **اللوجيك وطريقة الحساب:**
    - حساب مجموع أيام الإقامة الفعلية للحجوزات المفلترة: `SUM(DATEDIFF(end_date, start_date))`.
    - حساب إجمالي الأيام المتاحة للوحدات الـ 24 النشطة في الفترة المحددة: `24 شقة * عدد الأيام في الفترة المحددة`.
    - **المعادلة:** `(مجموع أيام الإقامة الفعلية / إجمالي الأيام المتاحة) * 100`.
  - **الجداول والأعمدة:** `reservations` (`start_date`, `end_date`)، `units` (`status`).

#### 2. الرسوم البيانية التفاعلية (SVG Charts):
* **📈 منحنى الأداء المالي (Financial Trend - Line Chart):**
  - رسم خطي تفاعلي يقارن بين الإيرادات والمصروفات شهرياً على مدار آخر 6 أشهر لتوضيح التدفق النقدي وصحة الأعمال.
* **🏆 أعلى الوحدات إيراداً (Top Units - Bar Chart):**
  - رسم بياني بالأعمدة لأعلى 5 شقق تحقيقاً للإيرادات (مرتبة تنازلياً حسب الإيراد التراكمي المحسوب لكل شقة).
* **🍕 مصادر الحجوزات (Booking Sources - Donut Chart):**
  - رسم دائري يوضح النسب المئوية لمصادر الحجز:
    - **Airbnb:** الحجوزات بجدول `reservations` حيث `platform = 'airbnb'`.
    - **Gathern:** الحجوزات بجدول `reservations` حيث `platform = 'gathern'`.
    - **Direct (مباشر):** الحجوزات بجدول `bookings` حيث `platform` يمثل الحجوزات اليدوية.

---

### التبويب الثاني: العمليات الحية (Live Operations)
تركز هذه الشاشة على التشغيل اليومي والميداني لموظفي الاستقبال والنظافة والصيانة.

#### 1. حالة الشقق الحية (Live Property Grid):
* **اللوجيك وطريقة الحساب:** شبكة تفاعلية تحتوي على 24 مربعاً ملوناً تمثل الشقق الـ 24، وتتغير ألوانها لحظياً بناءً على:
  - 🟢 **جاهزة (Ready):** إذا كانت حالة الشقة في قاعدة البيانات `readiness_status = 'ready'` وليست مشغولة اليوم.
  - 🔴 **مشغولة (Occupied):** إذا كان تاريخ اليوم يقع بين تاريخ الدخول وتاريخ الخروج لأي حجز نشط للشقة.
  - 🟡 **تحتاج تنظيف (Cleaning Needed):** إذا خرج النزيل اليوم وتغيرت الحالة لـ `dirty` أو تحتاج تنظيف.
  - 🛠️ **في الصيانة (Maintenance):** إذا كانت حالة الشقة `readiness_status = 'maintenance'` أو بها تذكرة صيانة جارية.
* **الجداول والأعمدة:** `units` (`readiness_status`, `unit_name`) وجداول الحجوزات.

#### 2. جدول حركة اليوم (Today's Arrivals & Departures):
* **اللوجيك وطريقة الحساب:** جدولين سريعين:
  - **القادمون اليوم (Check-ins):** الحجوزات التي تبدأ اليوم: `start_date = TODAY`.
  - **المغادرون اليوم (Check-outs):** الحجوزات التي تنتهي اليوم: `end_date = TODAY`.
* **الجداول والأعمدة:** `reservations` (`start_date`, `end_date`, `unit_id`)، `bookings` (`checkin_date`, `checkout_date`).

#### 🚨 3. رادار الصيانة والأعطال العاجلة (Maintenance Radar):
* **اللوجيك وطريقة الحساب:**
  - استعلام يجلب عدد تذاكر الصيانة الجارية وغير المحلولة ذات الأهمية العالية أو العاجلة:
    `COUNT(*) FROM maintenance_tickets WHERE status != 'resolved' AND priority IN ('urgent', 'high')`.
  - **الميزة الذكية:** إذا كانت هناك شقة بها عطل عاجل مفتوح، ولديها حجز جديد يبدأ غداً أو بعد غد؛ يظهر تنبيه أحمر وامض (Critical Warning Badge) باسم الشقة وصورتها لتحذير المدير لسرعة إرسال فني الصيانة قبل وصول النزيل!
* **الجداول والأعمدة:** `maintenance_tickets` (`status`, `priority`, `unit_id`) وجداول الحجوزات القادمة.

---

### التبويب الثالث: أداء وربحية العقارات (Property Performance & Profitability)
جدول تحليلي عميق يساعد الملاك في اتخاذ القرارات الاستثمارية ومعرفة ربحية كل عقار بدقة.

#### 📊 1. جدول الربحية الحقيقية للشقة (True Property Profitability Table):
* **اللوجيك وطريقة الحساب:** جدول تفصيلي يرتب الشقق الـ 24 بالكامل ويحسب المعادلة التالية لكل شقة:
  - `الإيراد المحسوب للشقة` (مجموع إيرادات حجوزات التقويم + اليدوية التابعة للشقة).
  - `مصاريف الصيانة الفعلية للشقة` (مجموع التكلفة التقديرية لتذاكر الصيانة الخاصة بالشقة).
  - `مصاريف التشغيل والنظافة للشقة` (عدد حجوزات الشقة * 50 ر.س).
  - **صافي ربح الشقة الفعلي** = `إجمالي الإيرادات - مصاريف الصيانة - مصاريف التشغيل`.
* **الجداول والأعمدة:** `units`, `reservations`, `bookings`, `maintenance_tickets`.
* **خيارات التفاعل:** إمكانية ترتيب الجدول تنازلياً حسب الأكثر ربحية، أو تصاعدياً لمعرفة الشقق الخاسرة التي تستهلك مصاريف صيانة عالية تلتهم أرباحها.

---

### التبويب الرابع: مبيعات الـ CRM والتنبؤات (CRM Pipeline & Financial Forecasting)
يستفيد هذا التبويب من بيانات نظام الـ CRM لتوقع المبيعات القادمة وعقود الشركات طويلة المدى.

#### 🎯 1. قمع مبيعات الـ CRM (Sales Pipeline Funnel):
* **اللوجيك وطريقة الحساب:** رسم توضيحي لقمع المبيعات يجمع قيم الصفقات الجارية مع الشركات مصنفة حسب مرحلة المبيعات:
  - حساب مجموع قيم الصفقات المتوقعة: `SUM(amount) GROUP BY stage`.
  - المراحل تشمل: صفقات جديدة (New)، تفاوض (Negotiation)، عرض سعر (Proposal)، رابحة ومكتملة (Won/Paid).
* **الجداول والأعمدة:** `crm_deals` (`amount`, `stage`).

#### 🔮 2. بطاقة التنبؤ المالي الذكي (Future Forecasting Card):
* **الوجيك وطريقة الحساب:** فحص الحجوزات المستقبلية المؤكدة في قاعدة البيانات والتي ستبدأ في الشهر القادم.
  - حساب إجمالي قيمتها المتوقعة: `مجموع (ليالي الحجز المستقبلي * السعر التقديري للشقة)`.
  - وعرضها كرسالة تنبؤية للمدير: *"الإيرادات المتوقعة المؤكدة للشهر القادم هي: X,XXX ر.س بناءً على الحجوزات المسجلة حالياً"*.
* **الجداول والأعمدة:** جداول الحجوزات المفلترة بالتواريخ المستقبلية.

---

### التبويب الخامس: مؤشرات الموارد البشرية والرواتب (HR & Payroll Insights)
يلخص كفاءة القوة البشرية وتوزيع الورديات ومصروفات الرواتب.

#### 👥 1. مؤشرات كفاءة الموظفين (HR Analytics):
* **إجمالي الرواتب المدفوعة:** مجموع صافي الرواتب في آخر دورة رواتب معتمدة (`SUM(net_salary)` من جدول `hr_payroll_details`).
* **معدل الحضور والانصراف:** نسبة الأيام التي سجل فيها الموظفون حضوراً فعلياً من إجمالي أيام العمل المحددة في جدول `hr_attendance`.
* **توزيع ورديات العمل (Shifts Chart):** رسم بياني يوضح عدد الموظفين في كل شفت عمل (صباحي، مسائي) بناءً على جدول `hr_shifts`.

---

## ❓ ثالثاً: الأسئلة المحددة والذكية المطلوب طرحها على الشركة (Targeted Client Questions)

لإتمام عملية الربط بأعلى درجات الدقة والاحترافية، يُنصح بنسخ هذه الأسئلة وإرسالها للمسؤول التقني بالشركة أو العميل للحصول على إجابات معتمدة:

> ### ✉️ استفسارات برمجية هامة لإتمام بناء لوحة التحليلات والتحكم المالي:
>
> السلام عليكم يا بشمهندس،
> شغالين حالياً على المراحل البرمجية لبناء وتطوير لوحة التحليلات المتقدمة (لوحة الأداءات) وربطها بالـ 53 جدول في قاعدة البيانات. وعشان نطلع أرقام دقيقة وحقيقية 100% بدون أي محاكاة، محتاجين نأكد مع حضرتك 3 نقاط هامة:
>
> 1. **سعر الليلة الافتراضي للعقارات (iCal Pricing):**
>    بما أن حجوزات الـ iCal المستوردة تلقائياً من Airbnb و Gathern في جدول `reservations` بتنقل تواريخ الإقامة فقط بدون المبالغ المالية؛ هل نقوم بإضافة عمود `default_price_per_night` في جدول العقارات `units` لتسجيل سعر ليلة افتراضي لكل شقة عشان السيستم يحسب الإيرادات ديناميكياً (ليالي الحجز × سعر الشقة)؟ وإذا كانت الإجابة نعم، هل متوفر لديكم قائمة بالأسعار الحقيقية للشقق الـ 24 (مثل شقق السليمانية Boho Nest و Retro nest وغيرها) لرفعها مباشرة؟
>
> 2. **تكاليف الصيانة وتذاكر الأعطال:**
>    لحساب مؤشرات (إجمالي المصروفات وصافي ربح كل شقة) بدقة؛ هل يوجد تكلفة مالية فعلية للصيانة سيتم ربطها بجدول التذاكر `maintenance_tickets`؟ أم نبرمج تكلفة تقديرية ثابتة لكل عطل يتم حله بنجاح (مثلاً 120 ريال)؟ وبالمثل، هل نعتمد تكلفة تشغيل ونظافة ثابتة لكل حجز (مثلاً 50 ريال)؟
>
> 3. **دمج مصروفات الرواتب والمشتريات:**
>    هل ترغبون في دمج رواتب الموظفين الفعليين المسجلة في جدول `hr_payroll_details` وفواتير الموردين (المشتريات) من جدول `accounting_invoices` لتُحسب تلقائياً وتُطرح ضمن (إجمالي المصروفات الكلية للشركة) في لوحة التحكم الرئيسية؟
>
> إجاباتكم الكريمة هتساعدنا نخلي لغة الأرقام في لوحة التحكم حقيقية وديناميكية بالملي ومطابقة لأعمالكم الفعلية على أرض الواقع!

---

### 💡 ما هي الخطوة التالية؟
بمجرد مراجعة هذا المخطط الدقيق وإعطائي **الموافقة (Approval)**، سأبدأ فوراً في إنشاء الأكواد وتعديل ملف `app/dashboard/page.tsx` وتطوير الرسوم البيانية التفاعلية للبدء في التنفيذ! 🚀🔥
