# الدليل الشامل لهيكل قاعدة البيانات ومنطق العمل (Database Architecture & Business Logic)

أهلاً بك يا صديقي! هذا الدليل هو **خارطة الطريق الكاملة** لنظام Nested United ERP. يحتوي النظام على **53 جدولاً** مصممة بدقة عالية لربط الحسابات المالية، والموارد البشرية، والـ CRM، والصيانة، وإدارة العقارات والحجوزات.

تم تقسيم الجداول هنا إلى **6 مجموعات رئيسية (Modules)**. لكل مجموعة، ستجد شرحاً مبسطاً باللغة العربية لمنطق العمل (Business Logic)، والعلاقات البرمجية، ووظيفة كل جدول بالتفصيل.

---

## 🏛️ الفهرس السريع للمجموعات (Modules)
1. [إدارة العقارات، الحجوزات، والمزامنة (Rentals & Sync)](#1-إدارة-العقارات-الحجوزات-والمزامنة-rentals--sync)
2. [النظام المالي والمحاسبة المزدوجة (Double-Entry Accounting)](#2-النظام-المالي-والمحاسبة-المزدوجة-double-entry-accounting)
3. [الموارد البشرية والرواتب (HR & Payroll)](#3-الموارد-البشرية-والرواتب-hr--payroll)
4. [إدارة المبيعات والعملاء (CRM & Sales Pipeline)](#4-إدارة-المبيعات-والعملاء-crm--sales-pipeline)
5. [التشغيل والصيانة والأعطال (Operations & Maintenance)](#5-التشغيل-والصيانة-والأعطال-operations--maintenance)
6. [صلاحيات النظام، الأمان، والتحكم (Security & System Core)](#6-صلاحيات-النظام-الأمان-والتحكم-security--system-core)

---

## 1. إدارة العقارات، الحجوزات، والمزامنة (Rentals & Sync)

### 💡 منطق العمل (Business Logic):
النظام مصمم لإدارة الفنادق والشقق المفروشة. يتم سحب الحجوزات تلقائياً من منصات مثل **Airbnb** و **Gathern** عبر بروتوكول الـ **iCal** (مزامنة التقويمات)، أو إدخالها يدوياً.
الهدف الأساسي هو منع التعارض في الحجوزات (Overbooking) وتحديث جاهزية الغرف فوراً بعد خروج النزيل للبدء في عمليات التنظيف والصيانة.

### 📋 الجداول وتفاصيلها:

#### 1. `units` (العقارات/الشقق)
- **الوظيفة:** الملف التعريفي لكل شقة أو وحدة فندقية.
- **الحقول الرئيسية:** الاسم (`unit_name`)، المدينة (`city`)، الطاقة الاستيعابية (`capacity`)، الحالة (`active`/`inactive`)، وحالة الجاهزية (`readiness_status` مثل جاهز، يحتاج تنظيف، صيانة).

#### 2. `reservations` (الحجوزات المستوردة تلقائياً - iCal)
- **الوظيفة:** يحتوي على الحجوزات المستوردة من Airbnb و Gathern عبر التقويم المزامَن.
- **الحقول الرئيسية:** معرف الوحدة (`unit_id`)، المنصة (`platform`)، تاريخ البدء (`start_date`)، تاريخ الانتهاء (`end_date`)، معرف حساب المنصة (`platform_account_id`).
- **💡 ملاحظة مهمة للتحليلات:** هذا جدول مهم للغاية للتحليلات لأنه يحتوي حالياً على **497 حجز حقيقي**، وهو الذهب الذي سنستخرج منه أرقام المبيعات والإشغال للعميل!

#### 3. `bookings` (الحجوزات اليدوية)
- **الوظيفة:** الحجوزات التي يقوم موظف الاستقبال بإدخالها يدوياً (مباشر، واتساب، هاتف).
- **الحقول الرئيسية:** اسم النزيل (`guest_name`)، الهاتف (`phone`)، مبلغ الحجز الفعلي (`amount`)، العملة (`currency` - الافتراضي SAR)، وتواريخ الدخول والخروج.

#### 4. `platform_accounts` (حسابات المنصات)
- **الوظيفة:** تسجيل أسماء الملاك أو الحسابات المسؤولة عن العقارات على المنصات المختلفة.
- **الحقول الرئيسية:** اسم الحساب (`account_name` مثل سليمان البازعي، فيصل العتيبي)، والمنصة التابع لها.
- **💡 منطق الربط:** نفس الشخص (مثل سليمان البازعي) قد يتكرر اسمه مرتين في هذا الجدول؛ مرة لحسابه في Airbnb ومرة لحسابه في Gathern. خوارزميتنا للفلترة ستجمع الاثنين معاً بذكاء عند البحث باسمه!

#### 5. `unit_calendars` (روابط تقويمات الوحدات)
- **الوظيفة:** يخزن روابط الـ iCal الخاصة بكل شقة على كل منصة ليقوم النظام بسحب الحجوزات منها دورياً.

#### 6. `unit_platforms` (منصات الوحدات)
- **الوظيفة:** جدول ربط يحدد ما إذا كانت الشقة معروضة على Airbnb أو Gathern أو كليهما.

---

## 2. النظام المالي والمحاسبة المزدوجة (Double-Entry Accounting)

### 💡 منطق العمل (Business Logic):
يعتمد النظام على **المحاسبة المزدوجة (Double-Entry Accounting)**؛ أي أن كل حركة مالية لها طرفان (مدين Debit ودائن Credit) بمبالغ متساوية تماماً لضمان عدم حدوث أي فروقات مالية. 
عند تأكيد فاتورة عميل، يُنشأ "قيد يومية" يرحل المبالغ لحسابات الإيرادات والذمم المدينة تلقائياً.

### 📋 الجداول وتفاصيلها:

#### 7. `accounting_accounts` (دليل الحسابات - Chart of Accounts)
- **الوظيفة:** شجرة الحسابات المالية (أصول، خصوم، إيرادات، مصروفات، حقوق ملكية).
- **الحقول الرئيسية:** كود الحساب (`code` مثل 101001)، الاسم (`name` مثل البنك الأهلي، صندوق الكاش)، والنوع (`type` مثل asset_bank, liability_payable, revenue).

#### 8. `accounting_moves` (قيود اليومية - Journal Entries)
- **الوظيفة:** رأس قيد اليومية الذي يجمع الحركات المالية.
- **الحقول الرئيسية:** التاريخ (`date`)، المرجع (`ref` مثل رقم الفاتورة)، الوصف (`narration`)، الحالة (`posted` مرحّل أو `draft` مسودة)، وإجمالي المبلغ.

#### 9. `accounting_move_lines` (سطور قيود اليومية - Journal Items)
- **الوظيفة:** السطور التفصيلية لكل قيد يومية (المدين والدائن).
- **الحقول الرئيسية:** معرف القيد (`move_id`)، معرف الحساب (`account_id`)، قيمة المدين (`debit`)، وقيمة الدائن (`credit`).
- **💡 قاعدة ذهبية:** مجموع الـ `debit` يجب أن يساوي مجموع الـ `credit` لكل قيد.

#### 10. `accounting_invoices` (الفواتير)
- **الوظيفة:** فواتير العملاء (Customer Invoices) وفواتير الموردين (Vendor Bills).
- **الحقول الرئيسية:** رقم الفاتورة (`invoice_number`)، النوع (`invoice_type`)، العميل/المورد (`partner_id`)، المبلغ الإجمالي (`total_amount`)، الضريبة (`tax_amount`)، والمبلغ المتبقي غير المدفوع (`amount_due`).

#### 11. `accounting_invoice_lines` (سطور الفواتير)
- **الوظيفة:** العناصر التفصيلية المباعة داخل الفاتورة (مثل: إيجار شقة، رسوم تنظيف إضافية) والأسعار والضرائب المطبقة عليها.

#### 12. `accounting_payments` (سندات القبض والصرف)
- **الوظيفة:** تسجيل استلام النقدية من العملاء (سند قبض) أو دفع المبالغ للموردين (سند صرف).
- **الحقول الرئيسية:** المبلغ (`amount`)، طريقة الدفع (`payment_method` كاش، شبكة، تحويل بنكي)، والتاريخ.

#### 13. `accounting_payment_allocations` (تخصيص الدفعات)
- **الوظيفة:** ربط سندات القبض بالفواتير المفتوحة (مثال: سداد جزء من قيمة فاتورة معينة).

#### 14. `accounting_journals` (دفاتر اليومية)
- **الوظيفة:** دفاتر التقسيم المالي (دفتر البنك، دفتر الصندوق، دفتر المبيعات، دفتر المشتريات).

#### 15. `accounting_partners` (الشركاء)
- **الوظيفة:** قائمة العملاء والموردين والجهات الخارجية التي يتم التعامل المالي معها.

#### 16. `accounting_cost_centers` (مراكز التكلفة)
- **الوظيفة:** لتوزيع المصاريف والإيرادات على أقسام معينة أو شقق معينة لمعرفة ربحية كل شقة على حدة.

#### 17. `accounting_audit_logs` (سجلات المراجعة المالية)
- **الوظيفة:** تسجل كل عملية تعديل أو إضافة مالية لأغراض الحماية ومنع التلاعب المحاسبي.

---

## 3. الموارد البشرية والرواتب (HR & Payroll)

### 💡 منطق العمل (Business Logic):
يقوم النظام بمتابعة الموظفين (عقود، حضور وانصراف، إجازات) وبناءً عليه يتم احتساب مسيرات الرواتب تلقائياً في نهاية الشهر. 
يحتوي احتساب الرواتب على: **(الراتب الأساسي + البدلات - الخصومات والغيابات - نسبة التأمينات الاجتماعية GOSI صاحب العمل والموظف)**.

### 📋 الجداول وتفاصيلها:

#### 18. `hr_employees` (ملفات الموظفين)
- **الوظيفة:** يحتوي على بيانات الموظفين الـ 17 (الاسم، المسمى الوظيفي، الراتب الأساسي، البدلات، البريد الإلكتروني).

#### 19. `hr_attendance` (الحضور والانصراف اليومي)
- **الوظيفة:** يسجل وقت حضور الموظف وانصرافه يومياً لحساب الغيابات وساعات التأخير.

#### 20. `hr_payroll_runs` (دورات تشغيل الرواتب)
- **الوظيفة:** تمثل الشهر المالي لتشغيل الرواتب (مثال: رواتب شهر مايو 2026).
- **الحقول الرئيسية:** تاريخ البدء والانتهاء، الحالة (`draft` مسودة، `approved` معتمد، `paid` مدفوع).

#### 21. `hr_payroll_details` (تفاصيل راتب كل موظف في الدورة)
- **الوظيفة:** تفصيل الراتب النهائي للموظف خلال الشهر (الأساسي والبدلات والاستقطاعات والتأمينات وصافي الراتب المستحق للدفع).

#### 22. `hr_settings` (إعدادات الـ HR)
- **الوظيفة:** تخزن النسب الثابتة مثل نسبة التأمينات الاجتماعية (GOSI) لصاحب العمل والموظف لحسابها بدقة.

#### 23. `hr_requests` (الطلبات والطلبات الإدارية)
- **الوظيفة:** طلبات الإجازات، القروض، السلف، التعويضات التي يقدمها الموظفون للموافقة عليها أو رفضها من الإدارة.

#### 24. `hr_shifts` (الورديات ومواعيد العمل)
- **الوظيفة:** تحديد مواعيد شفتات العمل (شفت صباحي، شفت مسائي) لحساب التأخير بدقة.

#### 25. `hr_payroll_run_logs` (سجل حركات الرواتب)
- **الوظيفة:** لحفظ سجل الأمان؛ مين اعتمد الرواتب وفي أي ساعة وبأي ملاحظة.

#### 26. `hr_evaluations` (التقييمات الشهرية)
- **الوظيفة:** تقييم أداء الموظفين بناءً على معايير الجودة والالتزام بالعمل.

#### 27. `hr_employee_eval_config` & `hr_evaluation_criteria` & `hr_evaluation_scores` & `hr_evaluation_templates`
- **الوظيفة:** جداول مساعدة لإعداد قوالب التقييم، وتوزيع الدرجات والمعايير وتخزين النتائج النهائية.

#### 28. `hr_announcements` (الإعلانات الإدارية)
- **الوظيفة:** الإعلانات الموجهة من الإدارة لجميع الموظفين لتظهر في لوحة التحكم الخاصة بهم.

#### 29. `hr_employee_messages` (المراسلات الداخلية)
- **الوظيفة:** الرسائل الخاصة والتعاميم الفردية المتبادلة بين الموظفين والإدارة.

---

## 4. إدارة المبيعات والعملاء (CRM & Sales Pipeline)

### 💡 منطق العمل (Business Logic):
متابعة صفقات المبيعات والعملاء المحتملين الذين يرغبون في استئجار شقق لمدد طويلة أو إقامة عقود شراكة. يتم نقل الصفقات عبر لوحة Kanban تفاعلية بمراحل متتالية (صفقة جديدة -> تواصل -> عرض سعر -> تفاوض -> ربح -> تم الدفع -> مكتمل).

### 📋 الجداول وتفاصيلها:

#### 30. `crm_deals` (الصفقات والمبيعات)
- **الوظيفة:** يمثل الصفقة أو فرصة المبيعات الجارية.
- **الحقول الرئيسية:** الاسم، القيمة المتوقعة (`amount`)، المرحلة (`stage` مثل qualified, proposal, won, paid)، الأهمية (`priority`) وتاريخ الإغلاق المتوقع (`expected_close_date`).

#### 31. `crm_activities` (نشاطات الصفقات)
- **الوظيفة:** تسجيل التحركات والمهام الخاصة بالصفقة (مثل: مكالمة هاتفية تم إجراؤها، موعد اجتماع لمعاينة شقة، إلخ).

#### 32. `customers` (بيانات العملاء)
- **الوظيفة:** يخزن بيانات العميل المحتمل أو الحالي (الاسم، الشركة، الهاتف، البريد).

#### 33. `crm_custom_stages` (المراحل المخصصة)
- **الوظيفة:** تسميات وألوان مراحل الصفقات لتظهر باللغة العربية والإنجليزية بشكل مخصص في لوحة الـ Kanban.

#### 34. `crm_tags` & `crm_customer_tags` (الوسوم والتصنيفات)
- **الوظيفة:** لتصنيف العملاء والصفقات بـ Tags معينة (مثل: عميل VIP، شقق سكنية، عقود شركات) لتسهيل الفلترة والبحث.

#### 35. `crm_automation_rules` (قواعد الأتمتة)
- **الوظيفة:** قواعد برمجية لتشغيل أحداث تلقائية (مثل: إرسال بريد إلكتروني تلقائي للعميل بمجرد تحول الصفقة إلى مرحلة "تم إرسال عرض السعر").

#### 36. `crm_notifications` (تنبيهات المبيعات)
- **الوظيفة:** تنبيهات خاصة بموظفي المبيعات والصفقات المتأخرة أو المهام المطلوبة.

---

## 5. التشغيل والصيانة والأعطال (Operations & Maintenance)

### 💡 منطق العمل (Business Logic):
عند حدوث عطل في إحدى الشقق (مثل عطل في التكييف أو السباكة)، يتم فتح "تذكرة صيانة" وربطها بالشقة المتضررة وتحديد درجة أهميتها (منخفضة، متوسطة، عاجلة). ويتم إسنادها لعامل صيانة معين لمتابعة الإصلاح وكتابة تقرير التشغيل وتكلفته.

### 📋 الجداول وتفاصيلها:

#### 37. `maintenance_tickets` (تذاكر الصيانة)
- **الوظيفة:** تسجيل وإدارة الأعطال الفنية في الوحدات الـ 24.
- **الحقول الرئيسية:** معرف الوحدة (`unit_id`)، عنوان العطل (`title`)، الأهمية (`priority` مثل urgent)، الحالة (`open`, `in_progress`, `resolved`)، الموظف المسؤول (`assigned_to`)، وتاريخ الحل وسجل الملاحظات.

---

## 6. صلاحيات النظام، الأمان، والتحكم (Security & System Core)

### 💡 منطق العمل (Business Logic):
التحكم الكامل في حماية النظام وتوزيع صلاحيات الدخول (Role-Based Access Control - RBAC). لا يُسمح لموظف الصيانة برؤية الحسابات المالية أو الرواتب، ويقتصر حسابه على تذاكر الصيانة والجاهزية، بينما يمتلك الـ Super Admin صلاحيات مطلقة لرؤية لوحات التحليلات وتصدير التقارير.

### 📋 الجداول وتفاصيلها:

#### 38. `users` (المستخدمون والمدراء)
- **الوظيفة:** حسابات الدخول إلى لوحة التحكم (الاسم، البريد الإلكتروني، تشفير كلمة المرور، الدور البرمجي `role` مثل super_admin, hr_manager, accountant).

#### 39. `user_permissions` & `role_system_permissions` (صلاحيات الأدوار والصفحات)
- **الوظيفة:** جداول التحكم التفصيلية بالصلاحيات؛ تحدد أي دور برمجي مسموح له بدخول أي صفحة وتعديل بياناتها.

#### 40. `activity_logs` & `user_activity_logs` (سجلات النظام الأمنية)
- **الوظيفة:** تسجل كل ضغطة زر أو تعديل يقوم به أي مستخدم داخل لوحة التحكم لضمان الشفافية والمراقبة.

#### 41. `notifications` (الإشعارات العامة)
- **الوظيفة:** إشعارات النظام الموجهة للمستخدمين (مثل: "تم تسجيل حجز جديد في شقة 17").

#### 42. `company_settings` (إعدادات الشركة)
- **الوظيفة:** حفظ بيانات الشركة الرئيسية (الاسم، السجل التجاري، العملة الرسمية، العنوان، الشعار/اللوجو).

#### 43. `sync_logs` (سجلات المزامنة الحية)
- **الوظيفة:** تسجل نتائج وتواريخ مزامنة تقويمات Airbnb و Gathern لمعرفة آخر توقيت مزامنة ناجح وأي أخطاء حدثت في الشبكة.

#### 44. `session_health_logs` (سجلات صحة الجلسات)
- **الوظيفة:** لمراقبة استقرار وسرعة اتصال النظام بقواعد البيانات والشبكة الخارجية.

#### 45. `browser_accounts` & `browser_notifications` & `platform_messages`
- **الوظيفة:** جداول مخصصة لإدارة حسابات المتصفح الداخلي والرسائل المزامنة من المنصات وإشعارات الرسائل الفورية الواردة من النزلاء.

---

### 🚀 كيف سنربط هذا بالتحليلات (Dashboard) المطلوبة؟
الآن وبعد أن فهمنا تركيبة الـ 53 جدولاً، سنقوم بسحب الخيوط التالية بذكاء لبناء لوحة الـ Zamra Hub:
1. **عدد الحجوزات، معدل الإقامة، ومعدل إشغال الوحدات:** سنحسبها بالكامل من جدول `reservations` وجدول `bookings` بعد ربطها بجدول العقارات `units`.
2. **الإيرادات والمصروفات وصافي الدخل:** سنحسب الإيرادات من خلال دمج تواريخ الحجوزات بأسعار الليلة التقديرية لكل نوع عقار، والمصروفات من خلال دمج مصاريف النظافة لكل حجز مع قيم الصيانة من جدول `maintenance_tickets`.
3. **الفلترة الذكية حسب المالك/الحساب:** سنربط `reservations` بجدول `platform_accounts` ونجمع الحسابات التي تحمل نفس الاسم (مثل دمج Airbnb وسليمان البازعي مع Gathern سليمان البازعي) لنمنح العميل أدق فلترة ممكنة!
