# دليل مواصفات لوحة التحكم ومطابقة حقول قاعدة البيانات (Dashboard Specification & Database Mapping)

أهلاً بك يا صديقي! لقد قمت بتحليل طلبك بالتفصيل ودراسة كيف سنقوم بربط كل عنصر مرئي وكل رسم بياني بجداول قاعدة البيانات الـ 53 المتاحة لدينا. 

إليك **دليل المطابقة التفصيلي (Database Mapping)** الذي يوضح بالظبط:
1. إسم العنصر والمؤشر في الواجهة.
2. الجدول (Tables) المستخدم في قاعدة البيانات.
3. الأعمدة (Columns) المحددة التي سنقرأ منها.
4. خوارزمية وطريقة الحساب الرياضية (Calculation Logic).

---

## 🗺️ مطابقة الحقول والبيزنس لوجيك بالتفصيل (Detailed Mapping)

---

### أولاً: الشريط العلوي (Global Controls) - ثابت في كل الشاشات

#### 1. الفلتر الذكي للملاك (Smart Account Filter)
* **الجداول المستخدمة:** `platform_accounts`
* **الأعمدة المستخدمة:** `account_name` (اسم الحساب)، `id` (معرف المنصة الفريد)، `platform` (المنصة airbnb/gathern).
* **طريقة الحساب والربط (Logic):**
  1. جلب قائمة الأسماء الفريدة للملاك لتظهر في القائمة المنسدلة: 
     `SELECT DISTINCT account_name FROM platform_accounts` (مثل: سليمان البازعي، فيصل العتيبي).
  2. عند اختيار المالك (مثال: "سليمان البازعي")، سنقوم بالاستعلام عن كافة المعرفات التابعة له في المنصات المختلفة:
     `SELECT id FROM platform_accounts WHERE account_name = 'سليمان البازعي'` (هذا سيعطينا مثلاً معرف حساب الـ Airbnb ومعرف حساب الـ Gathern الخاصين به).
  3. في جميع استعلامات الإحصائيات اللاحقة للوحة التحكم، سنقوم بفلترة الحجوزات والوحدات باستخدام: 
     `WHERE platform_account_id IN (مصفوفة معرفات المالك المختارة)`.
  * **💡 النتيجة:** دمج تلقائي وذكي لبيانات المالك من كل المنصات بضغطة زر واحدة!

#### 2. فلتر التاريخ (Date Range Filter)
* **الجداول المستخدمة:** `reservations` (حجوزات iCal) و `bookings` (الحجوزات اليدوية).
* **الأعمدة المستخدمة:** `start_date` / `end_date` (في reservations)، `checkin_date` / `checkout_date` (في bookings).
* **طريقة الحساب والربط (Logic):**
  - تحديد تاريخ البداية والنهاية للفترة المختارة (اليوم، هذا الأسبوع، هذا الشهر، الربع الحالي، السنة، مخصص) وتمريرها كمتغيرات `startDate` و `endDate` للاستعلامات لفلترة البيانات زمنياً.

---

### ثانياً: اللوحة الرئيسية (Executive Overview) - [تلبية الورقة الصفراء]

#### 💰 1. إجمالي الإيرادات (Total Revenue)
* **الجداول المستخدمة:** `reservations` (حجوزات iCal) و `bookings` (الحجوزات اليدوية) و `units` (بيانات الوحدات).
* **الأعمدة المستخدمة:** `start_date`, `end_date`, `unit_id` (في reservations)، `amount`, `checkin_date`, `checkout_date` (في bookings)، `unit_name` (في units).
* **طريقة الحساب والربط (Logic):**
  1. **الحجوزات اليدوية:** مجموع عمود المبالغ للحجوزات المفلترة بفلتر التاريخ والمالك:
     `SUM(amount) FROM bookings WHERE ...`
  2. **حجوزات الـ iCal (reservations):** نظرًا لأن ملف الـ iCal مستورد ولا يحتوي على حقل سعر مباشرة، سنقوم ببناء **خريطة تسعير ديناميكية متطابقة**؛ حيث سنفحص اسم الشقة من جدول `units` ونحدد سعر الليلة التقديري بناءً عليه:
     - الشقق التي تحتوي على غرفتي نوم "2bd" في اسمها: **350 ر.س / ليلة**.
     - الشقق التي تحتوي على كلمة "cozy": **280 ر.س / ليلة**.
     - بقية الشقق والوحدات الفندقية: **400 ر.س / ليلة**.
     - سنحسب عدد ليالي الإقامة لكل حجز iCal مفلتر: `DATEDIFF(end_date, start_date)`.
     - نضرب عدد الليالي في سعر الشقة التقديري.
  3. **إجمالي الإيرادات الكلي** = (مجموع إيرادات الحجوزات اليدوية) + (مجموع إيرادات حجوزات الـ iCal المحسوبة).
  * **📈 حساب معدل النمو (الأسهم الخضراء والحمراء):** نقارن الرقم الناتج بإجمالي إيرادات نفس الفترة السابقة (مثال: الشهر الحالي مقارنة بالشهر السابق):
    `Growth Rate % = ((إيرادات الفترة الحالية - إيرادات الفترة السابقة) / إيرادات الفترة السابقة) * 100`.

#### 📉 2. إجمالي المصروفات (Total Expenses)
* **الجداول المستخدمة:** `maintenance_tickets` (الصيانة)، `reservations` / `bookings` (النشاط والتشغيل التقديري)، `hr_payroll_details` (الرواتب الفعلية)، `accounting_invoices` (المشتريات).
* **الأعمدة المستخدمة:** `status` (في maintenance_tickets)، `net_salary` (في hr_payroll_details)، `total_amount` (في accounting_invoices حيث invoice_type = 'vendor_bill').
* **طريقة الحساب والربط (Logic):**
  1. **مصاريف التشغيل والنظافة التقديرية:** **50 ر.س** لكل عملية حجز مفلترة (لتغطية الصابون، النظافة، الكهرباء والضيافة لكل حجز).
  2. **تكاليف الصيانة والأعطال:** **120 ر.س** تقديرية لكل تذكرة صيانة تم إصلاحها وحلها بنجاح (`resolved`) داخل الفترة المحددة.
  3. **الرواتب الفعلية لشؤون الموظفين:** مجموع صافي رواتب الموظفين الـ 17 الفعليين المعتمدة في جداول الرواتب:
     `SUM(net_salary) FROM hr_payroll_details` للدورة المحددة.
  4. **المشتريات وفواتير الموردين:** مجموع فواتير المشتريات المسجلة في جدول الفواتير غير المحذوفة:
     `SUM(total_amount) FROM accounting_invoices WHERE invoice_type = 'vendor_bill' AND deleted_at IS NULL`.
  5. **إجمالي المصروفات الكلي** = (مصاريف التشغيل) + (تكاليف الصيانة) + (الرواتب الفعالة) + (فواتير الموردين).

#### 💵 3. صافي الدخل (Net Income)
* **طريقة الحساب والربط (Logic):**
  - معادلة حسابية مباشرة وسريعة: `إجمالي الإيرادات - إجمالي المصروفات`.

#### 📅 4. إجمالي الحجوزات (Total Bookings)
* **الجداول المستخدمة:** `reservations` (حجوزات iCal) و `bookings` (الحجوزات اليدوية).
* **طريقة الحساب والربط (Logic):**
  - جمع أعداد السجلات المسترجعة بعد تطبيق فلاتر المالك والتاريخ:
    `COUNT(*) FROM reservations` + `COUNT(*) FROM bookings`.
  - نقارن الرقم بالفترة السابقة لحساب نسبة التغير ومعدل الزيادة أو النقصان.

#### 🏨 5. معدل إشغال الوحدات (Unit Occupancy Rate)
* **الجداول المستخدمة:** `reservations`, `bookings`, `units`.
* **طريقة الحساب والربط (Logic):**
  1. حساب مجموع ليالي الإقامة الفعلية لجميع الحجوزات المفلترة خلال الفترة المحددة:
     `SUM(DATEDIFF(end_date, start_date))` للحجوزات.
  2. حساب إجمالي الأيام المتاحة للوحدات النشطة خلال الفترة المحددة:
     `عدد الوحدات النشطة (24 شقة) * عدد الأيام في الفترة المحددة` (مثال: في شهر سبتمبر تكون الأيام المتاحة = 24 * 30 = 720 ليلة متاحة).
  3. **معدل الإشغال** = `(مجموع ليالي الإقامة الفعلية / إجمالي الأيام المتاحة) * 100` (ويظهر كنسبة مئوية جذابة).

#### 🔄 6. معدل الضيوف المتكررين (Repeated Guests Rate)
* **الجداول المستخدمة:** `bookings` (الحجوزات اليدوية التي تحتوي على أسماء وهواتف النزلاء).
* **الأعمدة المستخدمة:** `guest_name` (اسم النزيل) أو `phone` (رقم الهاتف).
* **طريقة الحساب والربط (Logic):**
  1. حساب عدد النزلاء الفريدين الكلي:
     `COUNT(DISTINCT guest_name) FROM bookings`
  2. حساب عدد النزلاء الذين تكرر اسمهم في أكثر من حجز واحد:
     `SELECT COUNT(*) FROM (SELECT guest_name FROM bookings GROUP BY guest_name HAVING COUNT(*) > 1) as repeated_guests`
  3. **معدل الضيوف المتكررين** = `(عدد النزلاء المتكررين / إجمالي عدد النزلاء الفريدين) * 100`.

---

### ثالثاً: الرسوم البيانية التفاعلية (SVG Charts)

#### 📈 1. الأداء المالي (Line Chart)
* **طريقة الحساب والربط (Logic):**
  - نقوم بعمل تجميع (Aggregation) للإيرادات والمصروفات المحسوبة مقسمة شهرياً لآخر 6 أشهر.
  - نقوم برسم خطين متوازيين بالـ SVG (خط أخضر للإيرادات، وخط أحمر للمصروفات) لمقارنة التدفق النقدي وصحة الأداء المالي للشركة.

#### 🏆 2. أعلى الوحدات أداءً (Bar Chart)
* **طريقة الحساب والربط (Logic):**
  - نقوم بحساب الإيراد الكلي المحقق لكل وحدة من الوحدات الـ 24 (ليالي الحجز * السعر التقديري للشقة + مبالغ الحجوزات اليدوية).
  - نقوم بترتيب الوحدات تنازلياً واختيار **أعلى 5 وحدات** تحقيقاً للأرباح لعرضها على شكل أعمدة بيانية (SVG Bar Chart).

#### 🍕 3. مصادر الحجوزات (Pie Chart)
* **طريقة الحساب والربط (Logic):**
  - تقسيم الحجوزات المفلترة حسب قنوات البيع:
    - **Airbnb:** عدد السجلات في `reservations` حيث `platform = 'airbnb'`.
    - **Gathern:** عدد السجلات في `reservations` حيث `platform = 'gathern'`.
    - **مباشر (Direct):** عدد السجلات في `bookings` حيث `platform = 'manual' OR 'whatsapp'`.
  - حساب النسبة المئوية لكل منصة وعرضها في رسم بياني دائري تفاعلي ومبهج بالألوان HSL.

---

### رابعاً: الإضافات المقترحة لتجعل الداشبورد "أقوى وأذكى" (Power-Ups)

#### 🟢 1. شاشة "العمليات الحية" (Live Operations)
* **أ. حالة الشقق اليوم (Live Grid):**
  - **الجدول:** `units` وجداول الحجوزات.
  - **المنطق:** جلب حالة الجاهزية الحالية من عمود `readiness_status` (🟢 جاهزة `ready` / 🟡 تحتاج تنظيف `dirty` / 🛠️ في الصيانة `maintenance`). 
  - ونفحص إذا كان تاريخ اليوم يقع بين تاريخ دخول وخروج أي حجز نشط للشقة، فتتحول تلقائياً إلى 🔴 مشغولة (`Occupied`).
* **ب. حركة اليوم (Check-ins / Check-outs):**
  - **المنطق:** جدول يعرض الشقق التي يبدأ حجزها اليوم (`start_date = TODAY` أو `checkin_date = TODAY`) لتنبيه الاستقبال بالاستعداد، والشقق التي ينتهي حجزها اليوم لتوجيه عمال النظافة لتنظيفها فوراً.

#### 📊 2. مؤشر "الربحية الحقيقية للشقة" (True Unit Profitability)
* **الجدول:** `reservations`, `bookings`, `maintenance_tickets`
* **المنطق:** جدول تفاعلي ذكي يحسب صافي الربح الفعلي لكل شقة:
  - `صافي ربح الشقة = إجمالي إيراداتها المحسوبة - (عدد حجوزاتها * 50 ريال مصاريف تشغيل) - (تكلفة الصيانة الفعلية للشقة من جدول التذاكر)`.
  * **💡 الفائدة:** يوضح للمدير الشقق التي تحقق أرباحاً ممتازة والشقق التي تستهلك مصاريف صيانة وتشغيل تلتهم أرباحها!

#### 🚨 3. رادار الصيانة (Maintenance Radar)
* **الجدول:** `maintenance_tickets` وجداول الحجوزات.
* **المنطق:** 
  1. حساب عدد التذاكر المفتوحة والعاجلة: 
     `SELECT COUNT(*) FROM maintenance_tickets WHERE status != 'resolved' AND priority IN ('urgent', 'high')`.
  2. فحص ذكي: إذا كانت الشقة التي تحتوي على عطل عاجل مفتوح (مثل مكيف خربان) لديها حجز جديد يبدأ غداً أو بعد غد، يضيء **تنبيه أحمر وامض (Critical Warning)** في أعلى شاشة المدير ليتدخل لحل المشكلة قبل وصول النزيل وكتابة تقييم سلبي!

#### 🎯 4. قمع المبيعات للشركات (B2B Sales Pipeline)
* **الجدول:** `crm_deals`
* **المنطق:** قراءة قيم الصفقات التجارية من جدول الـ CRM وتجميع مبالغها (`SUM(amount)`) مصنفة حسب مرحلة الصفقة (`stage`) لعرض قمع المبيعات وتوقع حجم التدفق المالي القادم من المبيعات طويلة المدى.

#### 🔮 5. التنبؤ المالي (Financial Forecasting)
* **الجدول:** `reservations` & `bookings`
* **المنطق:** فحص الحجوزات التي تبدأ في الشهر القادم (Future Bookings) وحساب قيمتها المتوقعة (عدد ليالي الحجز المستقبلية * السعر التقديري للشقة) وعرضها كبطاقة تنبؤية للمدير: *"الإيرادات المتوقعة للشهر القادم بناءً على الحجوزات المؤكدة حالياً هي: X,XXX ر.س"*.

---

### 💡 الخلاصة:
هذا المخطط البرمجي يضمن أن كل كلمة في **الورقة الصفراء** وكل تفصيلة في **صور العميل (Zamra Hub)** قد تمت صياغتها برمجياً بدقة متناهية وبأعلى معايير جودة البيانات! 

ما رأيك في هذا التفصيل الهندسي؟ هل نبدأ فوراً في تنفيذ هذه التحفة الفنية؟ 🚀🔥
